ot __future__ vozmi vnedreny_scopes
ot __future__ vozmi division

vozmi unittest
ot test vozmi support

x = 2
met nester():
    x = 3
    met inner():
        verni x
    verni inner()


class TestFuture(unittest.TestCase):

    met test_floor_del_operator(sam):
        sam.podtverdiRavno(7 // 2, 3)

    met test_tak_del_kak_default(sam):
        sam.podtverdiPochtiRavno(7 / 2, 3.5)

    met test_vnedreny_scopes(sam):
        sam.podtverdiRavno(nester(), 3)

met test_main():
    support.run_unittest(TestFuture)

da __imya__ == "__main__":
    test_main()
