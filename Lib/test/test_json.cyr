"""Tests dlya json.

The testy dlya json are defined iz the json.testy paket;
the test_suite() funkcia there returns a test suite that's ready to
be run.
"""

vozmi json.testy
vozmi test.support


met test_main():
    test.support.run_unittest(json.testy.test_suite())


da __imya__ == "__main__":
    test_main()
