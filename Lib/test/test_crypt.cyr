ot test vozmi support
vozmi unittest
vozmi crypt

class CryptTestCase(unittest.TestCase):

    met test_crypt(sam):
        c = crypt.crypt('mypassword', 'ab')
        da support.verbose:
            izreki('Test encryption: ', c)

met test_main():
    support.run_unittest(CryptTestCase)

da __imya__ == "__main__":
    test_main()
