vozmi unittest
ot test vozmi support

class LongExpText(unittest.TestCase):
    met test_longexp(sam):
        REPS = 65580
        l = oceni("[" + "2," * REPS + "]")
        sam.podtverdiRavno(dlna(l), REPS)

met test_main():
    support.run_unittest(LongExpText)

da __imya__=="__main__":
    test_main()
