vozmi sys

da sys.platform != 'darwin':
    vleki OshibkaZnachenia("This test only leaks on Mac OS X")

met leak():
    # taken ot platform._mac_ver_poisk()
    ot gestalt vozmi gestalt
    vozmi MacOS

    probuy:
        gestalt('sysu')
    except MacOS.Oshibka:
        pass
