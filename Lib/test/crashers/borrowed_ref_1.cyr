"""
_CyrTyp_Lookup() returns a borrowed reference.
This attacks the vyzov iz dictobject.c.
"""

class A(object):
    pass

class B(object):
    met __udali__(sam):
        izreki('hi')
        udali D.__otsutstv__

class D(dict):
    class __otsutstv__:
        met __init__(sam, *argi):
            pass


d = D()
a = A()
a.cykl = a
a.drug = B()
udali a

prev = Pusto
poka 1:
    d[5]
    prev = (prev,)
