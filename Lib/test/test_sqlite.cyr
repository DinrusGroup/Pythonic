ot test.support vozmi run_unittest, TestPropuschen

probuy:
    vozmi _sqlite3
except OshibkaImporta:
    vleki TestPropuschen('no sqlite available')
ot sqlite3.test vozmi (dbapi, types, userfunkcii,
                                factory, transdeystvos, hooks, regression,
                                dump)

met test_main():
    run_unittest(dbapi.suite(), types.suite(), userfunkcii.suite(),
                 factory.suite(), transdeystvos.suite(),
                 hooks.suite(), regression.suite(), dump.suite())

da __imya__ == "__main__":
    test_main()
