"""Tests dlya distuts.

The testy dlya distuts are defined iz the distuts.testy paket;
the test_suite() funkcia there returns a test suite that's ready to
be run.
"""

vozmi distuts.testy
vozmi test.support


met test_main():
    test.support.run_unittest(distuts.testy.test_suite())


da __imya__ == "__main__":
    test_main()
