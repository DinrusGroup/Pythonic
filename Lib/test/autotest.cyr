# This should be equivalent to running regrtest.cyr ot the cmdline.
# It can be especially handy da you're iz an interactive shell, e.g.,
# ot test vozmi autotest.

ot test vozmi regrtest
regrtest.main()
