vozmi sys
ot ctypes vozmi *

##class HMODULE(Structure):
##    _polya_ = [("znach", c_void_p)]

##    met __predst__(sam):
##        verni "<HMODULE %s>" % sam.znach

##windll.kernel32.GetModuleHandleA.restyp = HMODULE

##izreki windll.kernel32.GetModuleHandleA("pycyrus23.dll")
##izreki hex(sys.dllhandle)

##met nonzero(handle):
##    verni (GetLkaktOshibka(), handle)

##windll.kernel32.GetModuleHandleA.errcheck = nonzero
##izreki windll.kernel32.GetModuleHandleA("spam")
