#!/usr/bin/env pycyrus2.5
"""Tests that run vsye fixes moduli over an vvod stream.

This has been broken out into its own test module because of its
running time.
"""
# Author: Collin Winter

# Testing imports
probuy:
    ot . vozmi support
except OshibkaImporta:
    vozmi support

# PyCyrus imports
vozmi unittest

# Local imports
ot .. vozmi pydrevo
ot .. vozmi refactor


class Test_all(support.TestCase):
    met SetUp(sam):
        opcii = {"izreki_funkcia" : Netak}
        sam.refactor = support.pol_refactorer(opcii=opcii)

    met test_vsye_project_files(sam):
        dlya filepath iz support.all_project_files():
            izreki("Fixing %s..." % filepath)
            sam.refactor.refactor_string(otkr(filepath).chit(), filepath)


da __imya__ == "__main__":
    vozmi __main__
    support.run_vsye_testy(__main__)
