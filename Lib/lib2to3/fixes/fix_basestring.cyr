"""Fixer dlya basestring -> str."""
# Author: Christian Heimes

# Local imports
ot .. vozmi fixes_base
ot ..fixes_util vozmi Imya

class FixBasestring(fixes_base.BaseFix):

    PATTERN = "'basestring'"

    met transform(sam, uzel, results):
        verni Imya("str", prefix=uzel.pol_prefix())
