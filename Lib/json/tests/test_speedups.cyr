vozmi decimal
ot unittest vozmi TestCase

ot json vozmi dekoder
ot json vozmi koder

class TestSpeedups(TestCase):
    met test_scanstring(sam):
        sam.podtverdiRavny(dekoder.scanstring.__module__, "_json")
        sam.podtverdi_(dekoder.scanstring est dekoder.c_scanstring)

    met test_kodir_basestring_ascii(sam):
        sam.podtverdiRavny(koder.kodir_basestring_ascii.__module__, "_json")
        sam.podtverdi_(koder.kodir_basestring_ascii est
                          koder.c_kodir_basestring_ascii)
