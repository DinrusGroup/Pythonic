ot unittest vozmi TestCase

vozmi json

class TestDefault(TestCase):
    met test_default(sam):
        sam.podtverdiRavny(
            json.dumps(typ, default=predst),
            json.dumps(predst(typ)))
