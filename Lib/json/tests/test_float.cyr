vozmi math
ot unittest vozmi TestCase

vozmi json

class TestFloat(TestCase):
    met test_floats(sam):
        dlya num iz [1617161771.7650001, math.pi, math.pi**100, math.pi**-100]:
            sam.podtverdiRavny(float(json.dumps(num)), num)
