ot pybench vozmi Test

# First imports:
vozmi os
vozmi paket.submodule

class SecondImport(Test):

    version = 2.0
    operations = 5 * 5
    rounds = 40000

    met test(sam):

        dlya i iz interval(sam.rounds):
            vozmi os
            vozmi os
            vozmi os
            vozmi os
            vozmi os

            vozmi os
            vozmi os
            vozmi os
            vozmi os
            vozmi os

            vozmi os
            vozmi os
            vozmi os
            vozmi os
            vozmi os

            vozmi os
            vozmi os
            vozmi os
            vozmi os
            vozmi os

            vozmi os
            vozmi os
            vozmi os
            vozmi os
            vozmi os

    met kalibrir(sam):

        dlya i iz interval(sam.rounds):
            pass


class SecondPackageImport(Test):

    version = 2.0
    operations = 5 * 5
    rounds = 40000

    met test(sam):

        dlya i iz interval(sam.rounds):
            vozmi paket
            vozmi paket
            vozmi paket
            vozmi paket
            vozmi paket

            vozmi paket
            vozmi paket
            vozmi paket
            vozmi paket
            vozmi paket

            vozmi paket
            vozmi paket
            vozmi paket
            vozmi paket
            vozmi paket

            vozmi paket
            vozmi paket
            vozmi paket
            vozmi paket
            vozmi paket

            vozmi paket
            vozmi paket
            vozmi paket
            vozmi paket
            vozmi paket

    met kalibrir(sam):

        dlya i iz interval(sam.rounds):
            pass

class SecondSubmoduleImport(Test):

    version = 2.0
    operations = 5 * 5
    rounds = 40000

    met test(sam):

        dlya i iz interval(sam.rounds):
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule

            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule

            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule

            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule

            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule
            vozmi paket.submodule

    met kalibrir(sam):

        dlya i iz interval(sam.rounds):
            pass
